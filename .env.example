# Auto-loaded from a project-local `.env` or `.umbraco-cli.env` file when present.
# Prefer `.umbraco-cli.env` when you want a CLI-specific project config file.
# `UMBRACO_BASE_URL` may be omitted if it can be resolved from
# `Properties/launchSettings.json` or `appsettings*.json`.
UMBRACO_BASE_URL=https://localhost:44391
UMBRACO_CLIENT_ID=umbraco-back-office-api-user
UMBRACO_CLIENT_SECRET=your-secret-here
UMBRACO_OUTPUT_FORMAT=
